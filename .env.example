# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Itwinetech@1234
DB_NAME=starthere
DB_PORT=3306

# Server
PORT=4002
CORS_ORIGINS=http://localhost:4200,http://localhost:8100
ADMIN_SOCKET_URL=http://localhost:4001

# JWT secret
JWT_SECRET=replace_with_a_strong_random_secret

# Crypto helper secret (if client-side encryption used)
CRYPTO_SECRET=replace_with_a_secret

# SMTP credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_smtp_password_or_app_password

# Rate limit
RATE_LIMIT_MAX=100

# Client URL used for reset links
CLIENT_URL=http://localhost:4200
